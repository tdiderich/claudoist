# Changelog

## v0.0.1 - 2026-01-30

- Node-based CLI replaces the Bash wrapper
- `init` scaffolds repo files and directory structure
- Calendar planning + Google OAuth now run in Node (no Python/.venv)
- Packaging via npm + Homebrew formula
- Added CLI inventory/architecture docs

All notable changes to this repo will be documented in this file.