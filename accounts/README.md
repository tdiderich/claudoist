# Accounts

Each customer lives under `accounts/<account-slug>/`.

Use `scripts/new_account.sh` to scaffold a new account folder from templates.
Account data is ignored by git by default; see `.gitignore`.
