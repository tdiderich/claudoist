# Data

This folder is for git-backed data that is safe to share publicly.

- `data/public/` is tracked by git and suitable for open source.
- `data/private/` is ignored by git for sensitive data.

If you need to share sanitized data, put it under `data/public/`.
