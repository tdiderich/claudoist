# Data Folder

Customer data lives here during local use. These files are intended to be git-friendly.

Default layout:

- data/customers/<customer_id>.json
- data/customers/<customer_id>-docs/<call_id>.md (optional)

Do not store secrets in this repo without encryption.
