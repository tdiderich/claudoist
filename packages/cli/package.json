{
  "name": "@claudoist/cli",
  "version": "0.1.0",
  "type": "module",
  "bin": {
    "claudoist": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm --prefix ../core run build && vitest run"
  },
  "dependencies": {
    "@claudoist/core": "file:../core",
    "commander": "^12.1.0",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.7",
    "typescript": "^5.5.4",
    "vitest": "^2.1.1"
  }
}
